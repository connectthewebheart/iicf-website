/** Repeating triangle bunting between the green Highlights band and Competitions (Figma "divider"). */
export function BuntingDivider() {
  return (
    <div
      aria-hidden="true"
      className="bunting relative z-10 h-[clamp(72px,10.5vw,152px)] -mt-[clamp(52px,7.6vw,109px)] -mb-[clamp(20px,2.8vw,40px)]"
    />
  );
}
